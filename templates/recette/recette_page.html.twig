{% extends "_template.html.twig" %}

{% block content %}
    <div class="dimension">
        <h2> Recette: {{ recette.name }}</h2>

        <div class="media-body">
                            <figure class="figure">
                               <img src="{{asset ('uploads/' ~ recette.brochure)}}" alt="Image" style="width: 300px; heigth: 300px">
                            </figure>
                        </div>
            <p>{{ recette.description }}</p>
        </div>

        <h3 class="subtitle">
            {# Albums: {{ artist.records|length }} #}
        </h3>
        {# {% include 'record/_record_list.html.twig' with {record_list: artist.records} %} #}
    
    </div>
{% endblock %}